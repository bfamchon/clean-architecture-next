pnpm hook:pre-push

if pnpx madge --extensions js,jsx,ts,tsx ./src --circular --ts-config tsconfig.json; then
  echo "✔️  Everything is fine, well played !"
else
  echo "⚠️  Circular dependencies errors are met, please fix them !"
fi
